package Simulation;

import World.WorldHandler;

public class Simulation {

    public Simulation(WorldHandler map, int delay, boolean writeToCSV, String filename){
        if(writeToCSV){
            // handle clearing file
            // every iteration call map.writeToCSV(filename)
        }
    }

    public void start(){
        // open window, simulate
    }
}
